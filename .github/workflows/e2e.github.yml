name: 'e2e github'
on:
  push:
    branches: [main]
jobs:
  test-e2e:
    name: Test e2e
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install envy globally
        run: npm ci && npm link
      - name: Create a React Native app
        run: cd .. && npx create-react-native-app example --yes --use-npm
      - name: Init envy
        run: react-native-envy init
        working-directory: ../example
      - name: Set dev environment
        run: npm run envy:set dev
        working-directory: ../example
      - name: Check that files exist
        run: ([ -e "./src/api.js" ] && [ -e "./settings.json" ] && echo 0 || echo 1) | exit
        working-directory: ../example
